// =========================================
// authRoutes.js - Punya: User 1 (Ketua/Lead)
// Tanggung jawab: Auth & User Management
// =========================================
// TODO (User 1):
//   - POST /auth/register  -> Daftarin user baru
//   - POST /auth/login     -> Login & dapet JWT token
//   - GET  /auth/profile   -> Liat profil user (butuh middleware JWT)
//   - PUT  /auth/profile   -> Edit profil user

import { Router } from 'express';
const router = Router();

// Tambahin endpoint lo di sini ya!
// Contoh:
// router.post('/register', registerController);
// router.post('/login', loginController);

export default router;
