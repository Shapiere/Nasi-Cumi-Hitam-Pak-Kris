// =========================================
// volunteerRoutes.js - Punya: User 4
// Tanggung jawab: Volunteer Recruitment
// =========================================
// TODO (User 4):
//   - POST   /volunteers              -> Daftar jadi relawan di suatu proyek
//   - GET    /volunteers/:projectId   -> Liat daftar relawan di proyek tertentu
//   - PUT    /volunteers/:id/approve  -> Approve relawan (khusus admin/owner proyek)
//   - DELETE /volunteers/:id          -> Batalin pendaftaran relawan

import { Router } from 'express';
const router = Router();

// Tambahin endpoint lo di sini ya!
// Contoh:
// router.post('/', joinProjectController);
// router.get('/:projectId', getVolunteersController);

export default router;
